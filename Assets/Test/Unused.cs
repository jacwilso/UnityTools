namespace QuickFind.Assets.Test {
    public class Unused {
        /*

                    searchField = new SearchField ();
            // searchField.downOrUpArrowKeyPressed += HandleSearchFieldArrowKey;
            searchControlId = (int) Type.GetType ("UnityEngine.GUIUtility,UnityEngine.dll")
                .GetMethod ("GetPermanentControlID", BindingFlags.Static | BindingFlags.NonPublic)
                .Invoke (null, null);

                        private static SearchField searchField;
            private static int searchControlId;


                        // Type.GetType ("UnityEditor.EditorGUI,UnityEditor.dll")
            //     .GetMethod ("EndEditingActiveTextField", BindingFlags.Static | BindingFlags.NonPublic)
            //     .Invoke (null, null);
            // EditorGUIUtility.editingTextField = false;

            // Rect r = new Rect (0, 0, 100, 100);
            // Type.GetType ("UnityEditor.EditorGUI,UnityEditor.dll")
            //     .GetMethod ("TextFieldInternal", BindingFlags.Static | BindingFlags.NonPublic, null, new [] { typeof (int), typeof (Rect), typeof (string), typeof (GUIStyle) }, null)
            //     .Invoke (null, new object[] { searchControlId, r, "Test", GUI.skin.textField });
            // EditorGUILayout.TextField (searchString);
            // return;

            // EditorGUIUtility.LookLikeInspector ();
            // GUILayout.BeginHorizontal (GUI.skin.FindStyle ("Toolbar"));
            // GUILayout.FlexibleSpace ();
            // searchString = GUILayout.TextField (searchString, GUI.skin.FindStyle ("ToolbarSeachTextField");
            // if (GUILayout.Button ("", GUI.skin.FindStyle ("ToolbarSeachCancelButton"))) {
            //     searchString = "";
            //     GUI.FocusControl (null);
            // }
            // GUILayout.EndHorizontal ();

            // var rect = GUILayoutUtility.GetLastRect ();
            // rect.x += style.padding.left;
            // rect.y += style.padding.top + style.margin.top;
            // rect.height -= style.padding.vertical + style.margin.vertical;
            // rect.width = rect.height;
            // GUI.DrawTexture (rect, icon, ScaleMode.ScaleToFit, true);

            // searchString = searchField.OnGUI (searchString);

            // var cancelButtonStyle = new GUIStyle ();
            // Rect searchRect = EditorGUILayout.GetControlRect ();
            // searchRect.height = textFieldStyle.CalcHeight (new GUIContent (""), 0);

            // EditorGUI.BeginChangeCheck ();
            // searchString = searchField.OnGUI (searchRect, searchString, textFieldStyle, cancelButtonStyle, cancelButtonStyle);
            // GUILayout.Space (searchRect.height - EditorGUIUtility.singleLineHeight);

            // if (EditorGUI.EndChangeCheck () && !string.IsNullOrEmpty (searchString)) {
            //     selectedField = -1;
            //     matching.Clear ();
            //     foreach (var path in AssetPaths) {
            //         if (path?.IndexOf (searchString, StringComparison.OrdinalIgnoreCase) >= 0) {
            //             matching.Add (path);
            //         }
            //     }
            // }

         */
    }
}